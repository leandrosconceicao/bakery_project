

import '../../../libraries/views.dart';

class IngredientManager extends StatelessWidget {
  final bool isEdit;
  const IngredientManager({super.key, this.isEdit=false});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        
      ],
    );
  }
}

// cost: 0,
// name: '',
// quantityInPackage: 0,
// storeCode: '',
// unitOfMeasurement: '',

